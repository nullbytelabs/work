# The dumb `work/agent` primitive: a Pi agent prompted entirely through a single
# `prompt` (or `promptFile`) — no agent package, no templating. Its final message
# is the step's single `output`. (Under test the agent runner is mocked, so this
# verifies the wiring without inference; a real model resolves from config.)
name: work-agent
jobs:
  summarize:
    runs-on: gondolin
    steps:
      - id: a
        uses: work/agent
        with:
          prompt: You are terse. Summarize what this project does in one line.
      - name: assert the agent produced an output
        env:
          OUT: ${{ steps.a.outputs.output }}
        run: |
          echo "agent said: $OUT"
          test -n "$OUT"
